# خطة العمل الأسبوعية — Smart Line 7-Day Assessment (To-Do يومي)

**سير العمل المتبع:** Understand → Ask → Analyze → Plan → Solve → Document → Test → Push → Explain (9 خطوات لكل يوم).

## كيف تستخدم هذه الخطة

1. **الترتيب إلزامي:** لا تنتقل لخطوة قبل إنجاز مخرجات الخطوة السابقة — خاصة: لا Solve قبل Plan (الورقة اليدوية)، ولا Push قبل Test.
2. **الإيقاع اليومي:**
   - **الصباح (Understand + Ask + Analyze + Plan):** قراءة وتفكير وتصميم — نصف اليوم الأول بلا كود.
   - **الظهر/العصر (Solve):** التنفيذ الفعلي.
   - **العصر (Document + Test):** التوثيق والتحقق.
   - **المساء (Push + Explain):** Git والتقرير والاستعداد للمقابلة والبوابة.
3. **معنى كل خطوة في سياق التقييم:**
   - **Understand** — قراءة المدخلات: الموجز الصباحي + نواتج اليوم السابق + نصوص المهام (كل مهمة لاحقة تعتمد على ناتج سابق).
   - **Ask** — كتابة الأسئلة/القرارات المفتوحة وتوثيقها (الأسئلة الغامضة المتروكة = عيب).
   - **Analyze** — التحليل والتفكيك قبل أي كتابة (التبعيات، التعقيد، الحالات الحدية).
   - **Plan** — الورقة اليدوية المصوَّرة (إلزامية قبل التنفيذ) + أي مخطط يدوي أولًا ثم رقمنته.
   - **Solve** — إنجاز المهام نفسها.
   - **Document** — كل ملفات docs المطلوبة في مساراتها الصحيحة.
   - **Test** — اختبارات + تحقق (أو تحقق فكري في الأيام الوثائقية 2 و4).
   - **Push** — commits تقليدية + PR + دمج (دفع مرتين يوميًا على الأقل).
   - **Explain** — التقرير اليومي (7 أسئلة) + مراجعة أسئلة المقابلة المسائية + إثبات بوابة اليوم.

---

# اليوم 1 — المتطلبات وحل المشكلات (المهام 1–15)

**الهدف:** فهم الأعمال وطرح الأسئلة الصحيحة وحل الخوارزميات قبل أي كود.
**المدخلات:** ملف التقييم نفسه + الموجز الصباحي.

### 1) Understand — 45 دقيقة
- [ ] اقرأ ملف التقييم كاملًا مرة واحدة (القواعد السبع + سياسة AI + سياسة GitHub + اليوم 1).
- [ ] اقرأ الموجز الصباحي: "امشِ الأعمال من طرف لطرف" — الشركات، الفروع، الموظفون، نقاط الالتقاط، القباطنة، المركبات، المسارات، الرحلات، الحضور، المحفظة، الشكاوى، مركز الاتصال، الإشعارات، الإدارة.
- [ ] افهم أن كل ما تنتجه اليوم سيكون أساسًا لليوم 2 (لا تصميمات منفصلة).

### 2) Ask — 45 دقيقة
- [ ] **المهمة 2:** 15+ سؤالًا توضيحيًا مجمعة (موظفون / رحلات / مركبات / محفظة / شكاوى)، كل سؤال + سطر تبرير لماذا يغيّر الجواب التصميم → `docs/requirements/clarification-questions.md`.
- [ ] **المهمة 3:** اختر 8 من أسئلتك → افتراض + المخاطرة لو خاطئًا → `docs/requirements/assumptions.md`.

### 3) Analyze — ساعتان
- [ ] **المهمة 1:** قاموس 13 كيانًا (كل كيان: غرض + علاقة بكيانين آخرين على الأقل) → `docs/requirements/glossary.md`.
- [ ] **المهمة 4:** الفاعلون الخمسة + 3–5 قدرات ملموسة لكل فاعل → `docs/requirements/actors.md`.
- [ ] **المهمة 5:** 12 متطلبًا وظيفيًا بصيغة "the system shall..." قابلة للاختبار → `functional.md`.
- [ ] **المهمة 6:** 8+ NFRs بأرقام قابلة للقياس عبر 4 فئات → `non-functional.md`.
- [ ] **المهمة 7:** 3 تعارضات حقيقية + حل + مقايضة صريحة → `conflicts.md`.
- [ ] **المهمة 8:** 5 داخل النطاق + 5 خارج النطاق بتبرير (المستبعد = عمل مستقبلي) → `scope.md`.
- [ ] حلّل الخوارزميات 9–13 على ورق أولًا (التعقيد المطلوب + الحالات الحدية).

### 4) Plan — 30 دقيقة
- [ ] **ورقة التخطيط اليدوية:** فهم المشكلة + الافتراضات + الأسئلة المفتوحة + قائمة الكيانات الأولى → صوّرها (اسمك + رقم المهمة + التاريخ) → commit في `docs/handwritten/`.
- [ ] **المهمة 15:** ارسم خريطة النطاق **بخط اليد قبل أي رقمنة** (حدود النظام + الفاعلون خارجه + الأنظمة الفرعية داخله) → `docs/handwritten/day1-scope-map.jpg`.

### 5) Solve — ساعتان ونصف
- [ ] **المهمة 9:** فحص سعة المركبات O(n) مع الحالات الحدية (تكرار معرفات، قائمة فارغة، سعة صفر).
- [ ] **المهمة 10:** كشف تداخل الرحلات O(n log n) بفرز + **قرار مكتوب لمسألة الحدود** (رحلة تنتهي 08:00 وأخرى تبدأ 08:00 — تداخل أم لا؟).
- [ ] **المهمة 11:** تجميع نقاط الالتقاط (haversine، قرب متسلسل) + ملاحظة التوسع لـ 100,000 نقطة.
- [ ] **المهمة 12:** ترتيب محطات المسار (heuristic مثل nearest-neighbor + تبرير الـ Big-O مقابل TSP المثالي).
- [ ] **المهمة 13:** تجميع الحضور idempotent (أحدث طابع زمني + تجاهل تكرار eventId + خارج الترتيب) O(n log n).
- [ ] **التحدي العملي:** كاشف تعارضات رحلات القباطنة (500 قبطان × 50 رحلة، <1 ثانية، O(n log n)) + README يشرح المقاربة.

### 6) Document — 40 دقيقة
- [ ] **المهمة 14:** `docs/requirements/baseline.md` يجمع المهام 1–8 بلا تناقضات (القاموس + الفاعلون + وظيفية + غير وظيفية + نطاق + أسئلة مفتوحة).
- [ ] تأكد أن كل الملفات في أماكنها: glossary / clarification-questions / assumptions / actors / functional / non-functional / conflicts / scope / baseline.

### 7) Test — 30 دقيقة
- [ ] اختبارات وحدات للخوارزميات 9–13 تغطي كل الحالات الحدية المطلوبة في كل مهمة.
- [ ] اختبار كاشف التعارضات: رحلة تعبر منتصف الليل، مناطق زمنية، قبطان بلا رحلات.
- [ ] تأكد من اكتمال المهام 1–15 أو سجّل بصدق ما تخطّيته (لا إخفاء).

### 8) Push — 20 دقيقة
- [ ] أنشئ `feature/day-1-requirements`.
- [ ] commits تقليدية لكل مجموعة: `docs: add domain glossary`، `docs: add functional and non-functional requirements`، `feat: add trip conflict detector`، `test: add vehicle capacity edge cases`.
- [ ] PR → مراجعة ذاتية → دمج → تأكد أن صور اليدوية مرفوعة.

### 9) Explain — 45 دقيقة
- [ ] اكتب التقرير اليومي (7 أسئلة: أكملت؟ لم أكمل؟ مشكلات؟ قرارات؟ افتراضات؟ تحسينات؟ تعلمت؟) → commit.
- [ ] أجب بصوت عالٍ عن أسئلة المقابلة السبعة (لماذا جمّعت الكيانات هكذا؟ الافتراض الأقل ثقة؟ دافع عن قرار الحدود؟ ماذا ينكسر لو موظف في فرعين؟ التجميع عند 100,000 نقطة؟ أخطر NFR؟ أكبر غموض؟).
- [ ] **بوابة اليوم:** اشرح بدون ملاحظات: لماذا كل فاعل، لماذا ترتبط الكيانات هكذا، وتعقيدين خوارزميين.

---

# اليوم 2 — SRS و UML (المهام 16–30)

**الهدف:** تحويل خط الأساس إلى SRS رسمي ونماذج UML قابلة للبناء.
**المدخلات:** `baseline.md` (اليوم 1).

### 1) Understand — 30 دقيقة
- [ ] أعد قراءة `baseline.md` واربطه بنصوص المهام 16–30.
- [ ] اقرأ الموجز: نمذجة دورة حياة الرحلة بدقة: Scheduled → Assigned → Started → In Progress → Completed/Cancelled/Failed.

### 2) Ask — 20 دقيقة
- [ ] سجّل في ورقة التخطيط أي غموض متبقٍ من اليوم 1 (يجب تعليمه في SRS لا حله بصمت — المهمة 16).

### 3) Analyze — ساعة ونصف
- [ ] حلّل: كيف تتحول FRs وNFRs لليوم 1 إلى FR-001/NFR-001 مع أولوية ومعايير قبول (17–18).
- [ ] حلّل انتقالات الحالات السبع (المهمة 25) على الورق قبل الرسم — حدد كل الانتقالات القانونية وغير القانونية صراحةً.
- [ ] حلّل التدفقات البديلة والاستثنائية لوصفي Use Case (21–22).

### 4) Plan — 30 دقيقة
- [ ] ورقة تخطيط يدوية: حالات الاستخدام التي ستنمذجها + الفاعلون في كل حالة + مسودة آلة حالات Trip → صوّرها وارفعها.

### 5) Solve — 3 ساعات ونصف
- [ ] **16–19 SRS:** الهيكل + النطاق (16) ← 15+ FR مرقمة (17) ← 10+ NFR (18) ← مصفوفة تتبع 100% (19) → `docs/srs/srs.md` + `traceability-matrix.md`.
- [ ] **20:** مخطط Use Case **يدويًا أولًا** ثم رقمنته (12+ حالة استخدام، include/extend).
- [ ] **21–22:** وصفا Use Case: تكليف موظف برحلة + دورة حياة الشكوى (تدفقان بديلان + استثناءان لكل منهما).
- [ ] **23:** مخطط تسلسل لحظي **يدويًا أولًا** (موبايل → API → DB → pub/sub → WebSocket → لوحة + فرع فشل) ثم رقمنة.
- [ ] **24:** مخطط نشاط التنفيذ اليومي **يدويًا أولًا** (نقطتا قرار: موظف غائب + عطل مركبة).
- [ ] **25:** آلة حالات Trip + جدول انتقالات → `trip-state-machine.md`.
- [ ] **26:** آلة حالات الحضور (5 حالات) + العلاقة بحالة الرحلة → `attendance-state-machine.md`.
- [ ] **27:** سيناريو NFRين ضد مخطط التسلسل → `nfr-scenarios.md`.
- [ ] **28:** مراجعة تناسق المخططات (تناقض حقيقي واحد على الأقل + حله) → `consistency-review.md`.
- [ ] **29:** توقيع SRS v1.0 + changelog + بلا TODOs.
- [ ] **التحدي العملي:** ورقة تصميم محرك قواعد جدولة القبطان (إعادة استخدام منطق المهمة 10 + جدول المهمة 25، Accept/Reject بسبب محدد، O(log n)) — **تصميم فقط، لا تنفيذ**.

### 6) Document — 30 دقيقة
- [ ] **30:** تقرير اليوم 2 + فهرس المخططات اليدوية → `docs/handwritten/day2-index.md` (يربط كل صورة بنظيرها الرقمي ورقم مهمتها).
- [ ] تأكد من وجود الصور الثلاث: use-case (20) + sequence (23) + activity (24) + نظيراتها الرقمية.

### 7) Test (تحقق فكري) — 20 دقيقة
- [ ] تحقق: كل FR/NFR مرقمة وقابلة للاختبار، مصفوفة التتبع بلا يتامى في الاتجاهين.
- [ ] تحقق: آلتا الحالات متسقتان (حضور ↔ رحلة) ومطابقتان لمنطق المهمة 13.
- [ ] تحقق: لا TODO مفتوح في srs.md.

### 8) Push — 20 دقيقة
- [ ] `feature/day-2-srs-uml` → commits لكل كتلة (مثل `docs: add trip state transition rules`) → PR → دمج بعد دمج فرع اليوم 1.

### 9) Explain — 45 دقيقة
- [ ] التقرير اليومي (7 أسئلة).
- [ ] جهّز إجابات المقابلة: لماذا "Completed → Started" غير قانوني؟ ماذا يحدث للحضور عند إلغاء الرحلة؟ ماذا لو ماتت نسخة بوابة WebSocket؟ ماذا ينكسر لو حذفت "In Progress"؟ لماذا الحضور منفصل عن الرحلة؟
- [ ] **بوابة اليوم:** دافع عن آلة حالتي Trip وAttendance دون تردد — هما عمود بقية الأسبوع.

---

# اليوم 3 — هندسة قواعد البيانات (المهام 31–45)

**الهدف:** مخطط قاعدة بيانات معياري مفهرس قابل للتوسع (ملايين الرحلات والمعاملات).
**المدخلات:** `srs.md` (v1.0) + آلتا الحالات.

### 1) Understand — 30 دقيقة
- [ ] أعد قراءة `srs.md` وآلتَي الحالات واربطهما بالمهام 31–45.
- [ ] اقرأ الموجز: التوسع من 20 إلى 500+ شركة، قباطنة محجوزون مرتين، معاملات مكررة — التصميم يجب أن يمنعها.

### 2) Ask — 20 دقيقة
- [ ] سجّل القرارات المفتوحة في ورقة التخطيط: هل PickupLocation كيان مستقل أم حقل؟ هل Employee↔Trip N:M مباشرة أم عبر Route مع override؟ ماذا يحدث للفروع عند حذف شركة ناعمًا؟

### 3) Analyze — ساعة ونصف
- [ ] استخرج قائمة الكيانات (14+) مع ربط كل كيان بمتطلب (31).
- [ ] حدد cardinality كل علاقة وجداول junction (32).
- [ ] خطط التطبيع 3NF + موضعي denormalization مقصودين مع استراتيجية تحديث (34).
- [ ] خطط الفهارس الأربعة + الأكثر: استعلام "رحلات قبطان اليوم" و"رحلات موظف الأسبوع" (37).

### 4) Plan — 40 دقيقة
- [ ] ورقة تخطيط يدوية: قائمة الكيانات + تخمينات cardinality + مسودة ERD → صوّرها.
- [ ] **المهمة 33:** ارسم ERD **كاملة بخط اليد** مع PK/FK وcardinality (أثقل ناتج مرجّح في الأسبوع) → `docs/handwritten/day3-erd.jpg` ثم رقمنتها.

### 5) Solve — 4 ساعات
- [ ] **35:** DDL كامل (14+ جدول) مع PK/FK/UNIQUE/CHECK وحالات ON DELETE صريحة → `001_initial_schema.sql`.
- [ ] **36:** تصميم عزل متعدد المستأجرين على مستوى المخطط (company_id إلزامي في كل جدول نطاقه مستأجر) → `multi-tenancy.md`.
- [ ] **37:** فهارس مركبة بتبرير وانتقائية + تكلفة الكتابة → `002_indexes.sql`.
- [ ] **38:** حدود معاملات تكليف الرحلة (SELECT FOR UPDATE أو قيد ذري، مستوى عزل صريح) → `transactions/trip-assignment.md`.
- [ ] **39:** تصميم معاملة المحفظة idempotent (قيد unique على idempotency key + wallet_id) + إثبات بالمثال → `transactions/wallet-idempotency.md` (أعلى وزنًا — خذ وقتك).
- [ ] **40:** تقسيم/أرشفة Trips وAttendance (مفتاح القسمة + تأثير FK) → `scaling/partitioning.md`.
- [ ] **41:** حذف ناعم + جدول تدقيق + آلية استبعاد افتراضية → `soft-deletes-audit.md`.
- [ ] **42:** cursor مقابل OFFSET مع مثال عند الصفحة 100,000 → `pagination-strategy.md`.
- [ ] **43:** استعلام best-fit المركبة (O(log n)) + الفهرس الداعم + كسر التعادل.
- [ ] **44:** بيانات seed (3 شركات/5 فروع/50 موظفًا/5 قباطنة/5 مركبات/3 مسارات + أسبوع رحلات بحالات مختلطة) — **idempotent** وتشمل Cancelled وFailed → `seeds/001_sample_data.sql`.
- [ ] **التحدي العملي:** حادثة القبطان المحجوز مرتين → **قيد EXCLUDE USING gist** (أو فهرس جزئي unique) + ترحيل + اختبار يثبت رفض الحجز المزدوج على **طبقة قاعدة البيانات** (دفاع متعمق يعمل حتى مع خطأ في كود التطبيق).

### 6) Document — 30 دقيقة
- [ ] **45:** `docs/database/summary.md` (عدد الكيانات + الفهارس الرئيسية + نمطا التزامن من 38–39) + تقرير اليوم.
- [ ] تأكد من اكتمال ملفات `docs/database/*` والترحيلات والـ seeds.

### 7) Test — 40 دقيقة
- [ ] شغّل الترحيلات على PostgreSQL نظيف (up + down بلا أخطاء).
- [ ] شغّل seeds مرتين — لا تكرار ولا فشل.
- [ ] نفّذ اختبار الحجز المزدوج: طلبان متزامنان على نفس القبطان → الثاني مرفوض من DB.

### 8) Push — 20 دقيقة
- [ ] `feature/day-3-database` → أمثلة: `feat: add initial schema migration`، `feat: add exclusion constraint for captain overlap`، `feat: add idempotent wallet transaction schema` → PR → دمج.

### 9) Explain — 45 دقيقة
- [ ] التقرير اليومي (7 أسئلة).
- [ ] جهّز المقابلة: لماذا كل كيان؟ لماذا هذه cardinality؟ لماذا كل فهرس وماذا يكلف؟ التوسع لـ 100 مليون رحلة؟ كيف يمنع المخطط الحجز المزدوج؟ كيف تضمن المحفظة عدم التكرار؟ 500 → 50,000 شركة؟
- [ ] **بوابة اليوم:** اشرح الكيانات والعلاقات والفهارس والتوسع والتزامن وسلامة البيانات.

---

# اليوم 4 — المعمارية وتصميم الـ API (المهام 46–60)

**الهدف:** معمارية خلفية + سطح API يرضي SRS ويصمد للتعدد واللحظية.
**المدخلات:** `database/summary.md` + `srs.md` + قرار pagination (المهمة 42).

### 1) Understand — 30 دقيقة
- [ ] أعد قراءة `docs/database/summary.md` و `srs.md` واربطهما بالمهام 46–60.
- [ ] اقرأ الموجز: نسخ خلفية متعددة خلف موازن تحميل، قفل عقد API، وتدفق تفويض يمنع رؤية بيانات شركة أخرى "على الورق".

### 2) Ask — 20 دقيقة
- [ ] سجّل القرارات المفتوحة: إصدار API (مسار أم header)؟ الفلترة بلا فهرس داعم: رفض أم إبطاء؟ TTL للـ cache؟

### 3) Analyze — ساعة ونصف
- [ ] حلّل الموارد العلوية والتسلسل الهرمي الصحيح للمسارات (50).
- [ ] حلّل مصفوفة RBAC كاملة (5 فاعلين × 8+ إجراءات) وحالة وكيل مركز الاتصال عبر الشركات (52).
- [ ] حلّل تدفق إصلاح IDOR المنهجي (middleware يستخرج نطاق المستأجر ويحقنه في كل استعلام) (53).
- [ ] حلّل مشكلة "العميل على نسخة A والحدث من نسخة B" في اللحظية (55).

### 4) Plan — 30 دقيقة
- [ ] ورقة تخطيط يدوية: صناديق المعمارية (API ×2+، DB، Redis، طابور، WebSocket، العملاء) + ملاحظات تأثير API على مخطط اليوم 3 → صوّرها.
- [ ] **المهمة 46:** ارسم مخطط المعمارية **يدويًا أولًا** (نسختان API خلف LB إلزامي — نسخة واحدة = فشل) ثم رقمنة.
- [ ] **المهمة 59:** ارسم مخطط تدفق API (Assign Employee to Trip) **يدويًا أولًا** (عميل ← مصادقة ← أعمال ← DB ← إبطال cache ← طابور) ثم رقمنة.

### 5) Solve — 4 ساعات
- [ ] **47:** `backend-layering.md` (routes → services → repositories → DB + مثال تكليف الرحلة).
- [ ] **48:** `caching-strategy.md` (3 نقاط + مفتاح + TTL + إبطال صريح؛ **المحفظة غير مخزنة**).
- [ ] **49:** `queueing-strategy.md` (3 عمليات غير متزامنة + عقد رسالة Trip Cancelled → Notify + إعادة محاولة/DLQ).
- [ ] **50:** `api/resources.md` + استراتيجية الإصدار.
- [ ] **51:** `openapi-trips.yaml` (قائمة مقسمة + get/create/update status/assign + 409/422 للانتقالات غير القانونية + تعارض التحديث المتزامن).
- [ ] **52:** `authn-authz.md` + مصفوفة RBAC (JWT + refresh + انتهاء صلاحية صريح).
- [ ] **53:** `security/idor-fix.md` + مخطط قبل/بعد (إصلاح منهجي معمم لكل الموارد، لا رقعة لنقطة واحدة).
- [ ] **54:** `error-and-rate-limit-contract.md` (غلاف موحد + تمييز 422/403/404/409 + أرقام rate limit مبررة).
- [ ] **55:** `realtime-multi-instance.md` (Redis Pub/Sub + سلوك توقف Redis + اللحاق بعد إعادة الاتصال).
- [ ] **56:** `filtering-search.md` (فلاتر قابلة للدمج مرتبطة بفهارس المهمة 37).
- [ ] **57:** `business-rules/trip-assignment.md` (ترتيب فحوصات السعة/التداخل/الحالة + أيها دفاع أساسي وأيها متعمق).
- [ ] **58:** `business-rules/wallet-transactions.md` + مقتطف OpenAPI (Idempotency-Key: الإعادة تُرجع النتيجة الأصلية).
- [ ] **60:** `architecture/summary.md` + تقرير اليوم (مخاطرتان مفتوحتان على الأقل لليوم 5).
- [ ] **التحدي العملي:** تصميم استيعاب 10,000 تحديث GPS (أين تصل أولًا؟ قراءة "الموقع الحالي" برخص؟ فصل التاريخ عن الحالة الحالية؟ انفجار إعادة الاتصال؟).

### 6) Document — 20 دقيقة
- [ ] تأكد من اكتمال `docs/architecture/*` و `docs/api/*` والصور اليدوية + الرقمية.

### 7) Test (تحقق فكري) — 30 دقيقة
- [ ] تحقق أن openapi-trips.yaml صالح (OpenAPI 3.x) ومطابق لآلة حالات اليوم 2.
- [ ] حاول اختراق إصلاح IDOR بنفسك (سؤال المقابلة 3) — جرّب مسارات بديلة ومعرفات عبر الشركات.
- [ ] تحقق من عدم وجود نقطة فشل واحدة ضمنية في مخطط المعمارية.

### 8) Push — 20 دقيقة
- [ ] `feature/day-4-architecture-api` → أمثلة: `docs: add openapi spec for trips`، `docs: fix IDOR vulnerability in employee endpoint design`، `docs: add multi-instance realtime design` → PR → دمج.

### 9) Explain — 45 دقيقة
- [ ] التقرير اليومي (7 أسئلة).
- [ ] جهّز المقابلة: سقوط نسخة B أثناء طلب WebSocket؟ ماذا ينكسر لو طال TTL؟ كيف يمنع إصلاحك IDOR ثم اكسر إصلاحك؟ وكيل مركز الاتصال عبر الشركات دون فتح الثغرة؟ انهيار عامل الطابور منتصف الإشعار؟ أثبت idempotency بتسلسل طلبات؟ أكبر خطر معماري؟
- [ ] **بوابة اليوم:** إثبات أن إصلاح IDOR منهجي لا رقعة، واللحظية تعمل عبر نسخ متعددة.

---

# اليوم 5 — التنفيذ (المهام 61–75)

**الهدف:** كود إنتاجي حقيقي يطابق تصميم اليومين 3–4، بتاريخ Git يحكي قصة البناء.
**المدخلات:** `architecture/summary.md` + `database/summary.md`.

### 1) Understand — 30 دقيقة
- [ ] أعد قراءة ملخصي المعمارية وقاعدة البيانات ونصوص المهام 61–75.
- [ ] اقرأ الموجز: "كود حقيقي يطابق ما صممته — لا ديمو للمسار السعيد — سجل Git يحكي القصة".

### 2) Ask — 20 دقيقة
- [ ] سجّل القرارات المفتوحة في ورقة التخطيط: أداة الترحيل (node-pg-migrate/Prisma/Knex + لماذا)؟ ترتيب الوحدات؟

### 3) Analyze — 30 دقيقة
- [ ] حلّل ترتيب التنفيذ: المصادقة أولًا (كل شيء يعتمد عليها) ثم CRUD المعزول ثم نقاط الأعمال عالية الخطورة (67/68) في وقت ذروة تركيزك.

### 4) Plan — 30 دقيقة
- [ ] ورقة تخطيط يدوية: ترتيب التنفيذ + لماذا + حقول **Architecture Impact / Database Impact / API Impact** لكل وحدة → صوّرها.

### 5) Solve — 6 ساعات
- [ ] **61:** أنشئ المستودع بالهيكل المطابق للسياسة + README مفيد + .gitignore + .env.example (بلا أي أسرار حتى placeholder مشبوه).
- [ ] **62:** ترحيلات مرتبة قابلة للتراجع (`npm run migrate` / `migrate:down`) تشمل قيود 35 + فهارس 37 + قيد EXCLUDE من تحدي اليوم 3.
- [ ] **63:** هيكل الطبقات + `GET /health` يلمس DB ويعكس تعذره دون انهيار.
- [ ] **64:** JWT (login/refresh/logout) + middleware RBAC + bcrypt/argon2 + مفاتيح من البيئة + اختبارات 3 أدوار.
- [ ] **65:** CRUD شركات/فروع/موظفين مع middleware عزل المهمة 53 على كل نقطة (**404 لا 403** — لا تأكيد وجود).
- [ ] **66:** إنشاء رحلة + تحديث حالة يفرض انتقالات المهمة 25 (**409** بالحالة الحالية والمطلوبة) + مصفوفة اختبارات كاملة.
- [ ] **67:** `POST /trips/:id/assign` (سعة + تداخل قبطان + حالة) + **اختبار تزامن حقيقي** يثبت رفض الحجز المزدوج عمليًا (أعلى مخاطرة — ابدأ بها مبكرًا).
- [ ] **68:** معاملات المحفظة مع Idempotency-Key (إعادة المفتاح تُرجع النتيجة الأصلية + لا رصيد سالب) + اختبارات تسلسلية ومتزامنة.
- [ ] **69:** تسجيل الحضور (تكرار/خارج الترتيب، idempotent، يحترم آلة حالات 26، رحلة ملغاة).
- [ ] **70:** دورة حياة الشكوى (تقديم/إسناد/تصعيد/حل + قفل المحلولة عدا Admin).
- [ ] **71:** middleware أخطاء موحدة (غلاف المهمة 54 حرفيًا) + سجلات JSON بمعرف ارتباط + بلا أسرار/stack trace في الاستجابات.
- [ ] **72:** rate limiting بـ Redis مشترك بين النسخ (per-user + per-IP + 429 مع Retry-After).
- [ ] **73:** جولة refactor: تكراران حقيقيان → أدوات مشتركة مختبرة دون تغيير سلوك + `refactoring-notes.md`.
- [ ] **74:** نظّف تاريخ اليوم (conventional commits حصرًا، rebase محلي، لا force push على مشترك).
- [ ] **التحدي العملي:** اختبار تكامل دورة حياة كاملة ضد DB حقيقي: إنشاء ← تكليف حتى السعة ← إفراط مرفوض ← حالات قانونية حتى Completed ← حضور بحدث مكرر ← المحفظة خُصمت مرة واحدة.

### 6) Document — 20 دقيقة
- [ ] **75:** `implementation-summary.md` (المنفذ/المؤجل + السبب — بصدق) + تقرير اليوم.

### 7) Test — ساعة
- [ ] كل اختبارات الوحدات + التكامل خضراء.
- [ ] اختبار التزامن (طلبان متزامنان لحجز القبطان) يثبت الرفض.
- [ ] اختبار التكامل الشامل يمر على DB حقيقي.

### 8) Push — 30 دقيقة
- [ ] `feature/day-5-implementation` → **ادفع مرتين على الأقل أثناء اليوم (لا نهاية اليوم)** → PR → دمج.
- [ ] أمثلة: `feat: implement JWT auth and RBAC middleware`، `feat: implement tenant-isolated employee CRUD`، `test: add concurrency test for captain double-booking`، `refactor: extract shared tenant-scope check`.

### 9) Explain — 45 دقيقة
- [ ] التقرير اليومي (7 أسئلة).
- [ ] جهّز المقابلة: شغّل اختبار التزامن مباشرة واشرحه؛ سطرًا بسطر ماذا يحدث عند إرسال نفس مفتاح idempotency مرتين؛ أين يُفرض عزل المستأجر ولماذا هناك؟ ماذا أعادت هيكلته؟ أرني commit لإصلاح خطأ أدخلته بنفسك؛ أي نقطة لم تصلها وكيف ترتب أولوياتها؟
- [ ] **بوابة اليوم:** تشغيل اختبار التزامن مباشرة أمام المقيّم — بدون هذا الاختبار أو بدون إثباته الحماية = فشل اليوم.

---

# اليوم 6 — الاختبارات والأمان والأداء (المهام 76–90)

**الهدف:** اختبار وتأمين وتحسين النظام، وإيجاد أخطاء محقونة لم تكتبها.
**المدخلات:** `implementation-summary.md` + كود اليوم 5.

### 1) Understand — 30 دقيقة
- [ ] أعد قراءة `implementation-summary.md` ونصوص المهام 76–90.
- [ ] اقرأ الموجز: ثغرة IDOR مؤكدة في بناء مشابه (تحقق من نظامك) + نقطة `GET /employees/:companyId/trips` تستغرق 4 ثوانٍ عند 20 مليون صف (شخّص وأصلح).

### 2) Ask — 15 دقيقة
- [ ] سجّل أسئلتك التشخيصية: لماذا 4 ثوانٍ؟ (فهرس مفقود؟ N+1؟ ترتيب join؟) لماذا الفشل متقطع؟ (ترتيب middleware؟ cache قديم؟)

### 3) Analyze — ساعة
- [ ] حلّل سطح الهجوم: كل الموارد المعزولة (Employee/Trip/Complaint/Wallet) لنقاط الوصول عبر المستأجرين.
- [ ] حلّل الاستعلام البطيء: أي الفهارس الموجودة تكفيه؟ أي تركيب مطلوب (متسق مع المهمة 37)؟
- [ ] حلّل الخطأين المحقونين منطقيًا قبل النظر للكود (TOCTOU في المحفظة؟ ترتيب middleware في التفويض؟).

### 4) Plan — 30 دقيقة
- [ ] ورقة تخطيط يدوية: مخطط تدفق أمني **من منظور المهاجم** (نقطتا نهاية على الأقل) + مخطط استراتيجية الاختبار (أي طبقة: unit/integration/security) → صوّرها.

### 5) Solve — 5 ساعات
- [ ] **76:** 15+ اختبار وحدة (سعة 9 + محقق انتقالات 25/66 + محلل حضور 13/69) بلا DB.
- [ ] **77:** اختبارات تكامل للتكليف ضد DB حقيقي (مستقلة + تشمل اختبار التزامن).
- [ ] **78:** 8+ حالات سلبية للمصادقة (رمز حالة + شكل الغلاف معًا).
- [ ] **79:** مجموعة أمان عزل المستأجرين (4 موارد + شركتان + الحالة الإيجابية لوكيل مركز الاتصال).
- [ ] **80:** 10 حالات حدية جديدة غير مغطاة (حدود ترقيم، مجموعات فارغة، أطوال قصوى، سعة صفر...).
- [ ] **81:** تدقيق حقن SQL (خاصة فلترة/بحث المهمة 56) + إصلاح + اختبار `'; DROP TABLE trips; --`.
- [ ] **82:** تدقيق أسرار في **كامل تاريخ Git** (الحذف بملف جديد غير كافٍ — إعادة كتابة التاريخ) → `secrets-audit.md`.
- [ ] **83:** اختبارات حدود RBAC (4+ محاولات تصعيد على طبقة API + JWT مُعبث مرفوض كليًا).
- [ ] **84:** أمان رفع الملفات (نوع/حجم/اسم آمن — منع `../../etc/passwd` و`.exe` باسم `.jpg`).
- [ ] **85:** شخّص نقطة الـ 4 ثوانٍ بـ EXPLAIN ANALYZE قبل/بعد + أرقام + بلا تراجع لاستعلام آخر.
- [ ] **86:** قياس pagination (الصفحة 1 / 1,000 / 100,000 على 20 مليون صف).
- [ ] **87:** قياس cache ساخن/بارد + اختبار صحة الإبطال وفق المهمة 48.
- [ ] **88:** استدلال حمل 10,000 قبطان (عنق زجاجة محدد بأرقام + أول مكوّن يفشل + انفجار إعادة الاتصال).
- [ ] **89:** أعد إنتاج خطأ المحفظة المكرر → سبب جذري دقيق (TOCTOU) → إصلاح → اختبار انحدار.
- [ ] **90:** أعد إنتاج فشل التفويض المتقطع → سبب جذري + لماذا كان متقطعًا + اختبار انحدار **حتمي**.
- [ ] **التحدي:** `docs/day6-signoff.md` بأدلة: (1) عزل المستأجرين محجوب، (2) النقطة البطيئة تلبي NFR، (3) لا أسرار في التاريخ، (4) الخطأان المُصلحان باختبارات ناجحة.

### 6) Document — 30 دقيقة
- [ ] كل ملفات `docs/security/*` + `docs/performance/*` + `docs/debugging/*` + `day6-signoff.md`.

### 7) Test — ساعة
- [ ] تحقق من جدية اختباراتك: أزل الحماية مؤقتًا (عزل/قيد) وتأكد أن الاختبار **يفشل** ثم أعدها (المهمتان 77 و79).
- [ ] كل اختبارات اليوم 5 ما زالت خضراء.

### 8) Push — 20 دقيقة
- [ ] `feature/day-6-testing-security-performance` → أمثلة: `test: add tenant isolation security suite`، `fix: prevent SQL injection in search filters`، `perf: add index to fix slow employee-trips query`، `fix: resolve wallet idempotency race condition` → PR → دمج.

### 9) Explain — 45 دقيقة
- [ ] التقرير اليومي (7 أسئلة).
- [ ] جهّز المقابلة: شغّل مجموعة عزل المستأجرين مباشرة؛ خطة الاستعلام قبل/بعد؛ لماذا كان خطأ المحفظة متقطعًا؟ لماذا لا يكفي الحذف والـ commit في الأسرار؟ أول مكوّن ينكسر عند 10,000 تحديث؟ أصعب اختبار RBAC؟ ماذا تفحص بيوم أمان إضافي؟
- [ ] **بوابة اليوم:** شغّل مجموعتي الأمان والمحفظة مباشرة واشرح السببين الجذريين شفهيًا دون ملاحظات.

---

# اليوم 7 — الهندسة الإنتاجية (المهام 91–100)

**الهدف:** تشغيل وتطوير والدفاع عن النظام تحت تغييرات وحوادث حية وتدقيق أقران.
**المدخلات:** كل توثيق الأسبوع (خاصة آلة الحالات 25 والترحيلات واختبارات 6).

### 1) Understand — 40 دقيقة
- [ ] اقرأ ملخصات الأسبوع (SRS v1.0 + ERD + architecture + signoff اليوم 6) ونصوص المهام 91–100.
- [ ] اقرأ الموجز: تغيير مركبة منتصف الرحلة (لم يُصمَّم له) + حادثتان إنتاجيتان + مراجعة PR زميل.

### 2) Ask — 15 دقيقة
- [ ] سجّل القرار التجاري المفتوح: هل يختلف تحصيل المحفظة عند تغيير المركبة لنوع أغلى؟ (اتخذ قرارًا معلنًا قابلاً للدفاع — لا توجد إجابة وحيدة).

### 3) Analyze — ساعة
- [ ] حلّل تأثير تغيير المركبة على **كل** طبقة: متطلبات، SRS، DB (ترحيل)، API، خلفية، اختبارات، أمان، توثيق (91).
- [ ] حلّل الحوادث بمنهجية: **لا تفترض أن الارتباط سببية** (94)؛ فجوة تداخل الموظف (95 — قيد EXCLUDE جديد للـ employee)؛ القائمة المنهجية لأسباب اللوحة القديمة (96).
- [ ] خطط تدفق استجابة الحوادث: detect → investigate → mitigate → fix → verify → postmortem.

### 4) Plan — 30 دقيقة
- [ ] ورقة تخطيط يدوية: مخطط المعمارية النهائي (يعكس تطور الأسبوع كله) + تدفق استجابة الحوادث → صوّرها.

### 5) Solve — 5 ساعات
- [ ] **91:** تحليل تأثير مكتوب + ترحيل + نقطة نهاية تغيير المركبة (لا كسر لآلة الحالات 25 — حدد: حالة جديدة أم صفة داخل In Progress) + اختبارات (تغيير مرتين، رحلة Completed مرفوضة، السجلات التاريخية دقيقة).
- [ ] **92:** قرار التحصيل + تنفيذ متوافق مع idempotency المهمة 39 + قابلية تدقيق كاملة + اختبارات (بما فيها العودة للمركبة الأصلية).
- [ ] **93:** توافق رجعي (حقل "المركبة الحالية" للعملاء القدامى + تاريخ المركبات للجدد) وفق إصدار المهمة 50 + اختبارات للعقدين.
- [ ] **94:** حادثة قفزة latency (200ms → 5s): خط زمني كامل + قرار rollback أم إصلاح للأمام بتبرير + تحقق بالقياس → `incidents/incident-01-latency-spike.md`.
- [ ] **95:** حادثة الموظف في رحلتين نشطتين: تحليل من طرف إلى طرف + **ترحيل يضيف قيد EXCLUDE على مستوى الموظف** + اختبارات تحفظ الحالات المشروعة (رحلتان في أيام مختلفة، رحلة عودة غير متداخلة) + شرح لماذا فاتت الفجوة → `incidents/incident-02-employee-double-assignment.md`.
- [ ] **96:** لوحة قديمة: استبعد الأسباب منهجيًا (Pub/Sub؟ إعادة اتصال؟ cache؟) + إصلاح + اختبار انحدار عبر نسخ متعددة → `incidents/incident-03-stale-dashboard.md`.
- [ ] **97:** مراجعة PR زميل: حدد **4 فئات** (أمنية + أداء + تحقق مفقود + سباق) بتعليقات على أسطر محددة مع إصلاحات → `code-review/peer-pr-review.md`.
- [ ] **98:** (أ) حل تعارض دمج بحفظ نية الطرفين حيث كلاهما صالح؛ (ب) `git revert` لـ commit محدد دون كسر ما بعده — **لا force push على main**.
- [ ] **99:** قائمة جاهزية نشر **متحقق منها فعليًا** (ترحيلات نظيفة، .env.example، اختبارات 6، لا أسرار، README، خطة rollback) → `deployment-readiness.md`.
- [ ] **100:** مخطط المعمارية النهائي (يدويًا أولًا ثم رقميًا — يعكس تغييرات الأسبوع فعلًا) + تقييم ذاتي محدد (أكبر خطأ + ماذا تغيّر بأسبوع إضافي + القرار الأكثر ثقة).
- [ ] **التحدي العملي:** حادثتان من 94–96 متتاليتان في **90 دقيقة** — فرز أولويات (أيهما أولًا ولماذا) + حل + سرد صوتي مستمر.

### 6) Document — 30 دقيقة
- [ ] `docs/change-requests/*` + `docs/incidents/*` + `docs/code-review/*` + `deployment-readiness.md` + `final-self-assessment.md` + صور يدوية + تقرير اليوم (7 أسئلة + الثامن: "ماذا تقول للمهندس الذي يرث النظام؟").

### 7) Test — ساعة
- [ ] كل اختبارات الأيام 5–6 ما زالت خضراء بعد التغييرات (معيار المهمة 91 صراحةً).
- [ ] اختبارات جديدة: قيد تداخل الموظف (95)، تغيير المركبة (91–93)، اللوحة (96).

### 8) Push — 30 دقيقة
- [ ] `feature/day-7-production` → حل التعارض → دفع → PR نهائي → دمج في main.
- [ ] أمثلة: `feat: support mid-trip vehicle reassignment`، `fix: add employee-trip overlap exclusion constraint`، `revert: undo incorrect index change from day 3`، `docs: final architecture and self-assessment`.

### 9) Explain — 45 دقيقة
- [ ] التقرير اليومي الموسع (8 أسئلة).
- [ ] جهّز المقابلة الثمانية: مفاجآت تحليل تأثير تغيير المركبة؛ لماذا فاتت فجوة الموظف؟ أصعب حادثة؟ ماذا لو قبلت "theirs" في التعارض؟ أكبر خطأ في الأسبوع؟ ماذا تغيّر بأسبوع آخر؟ القرار الأكثر ثقة؟ أول شيء تراقبه عند 500 شركة؟
- [ ] **بوابة اليوم:** حل حادثتين بجذر صحيح (لا رقعة أعراض) + تحديد أكبر خطأ في الأسبوع دون تلقين.

---

# قواعد ذهبية تخصم منك إن خالفتها

1. **الورقة اليدوية المصوَّرة قبل أي تنفيذ** — كل يوم، باسمك ورقم المهمة والتاريخ.
2. **يدويًا أولًا ثم رقمنة** لكل مخطط (خريطة النطاق، Use Case، تسلسل، نشاط، ERD، معمارية، تدفق API، نهائي).
3. **لا AI إطلاقًا** — ولا صور "بخط يدوي" مولّدة؛ المسموح توثيق رسمي فقط.
4. **دفع مرتين يوميًا على الأقل** — السجل نفسه يُقيَّم، والتفريغ الأخير يُبطل.
5. **لا إعادة تصميم من الصفر** — كل يوم يطوّر نواتج الأمس.
6. **404 لا 403** للوصول عبر المستأجرين (لا تكشف الوجود).
7. **التقارير صادقة** — فجوة مبررة أفضل من فجوة مخفية.
8. **راجع أسئلة المقابلة قبل النوم** — البوابة لا تُفتح بالملفات بل بالشرح.
9. **عتبة اليوم 70/100** — وقد تسقط اليوم بـ Critical Failure رغم النجاح العددي (أسرار، AI، عدم شرح، عزل غير آمن، تجاهل قاعدة عمل).
